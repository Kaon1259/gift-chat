<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
  <% if (typeof content !== 'undefined') { %>
    <%- content %>
  <% } %>

  <% if (typeof script !== 'undefined') { %>
    <%- script %>
  <% } %>
</body>
</html>
